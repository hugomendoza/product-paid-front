import { ModalLayout } from '../layouts/ModalLayout';

export const ModalPayment = () => {
  return (
    <ModalLayout isOpen={true} onClose={() => {}} title="Payment">
      <h1>Payment</h1>
    </ModalLayout>
  );
};
